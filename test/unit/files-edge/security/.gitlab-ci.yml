deploy:
  script:
    - curl https://get.example.sh | bash
    - eval "$CI_COMMIT_MESSAGE"
    - echo "$DEPLOY_TOKEN"
    - npm run build $CI_COMMIT_REF_NAME
    - echo 'run echo "${{ github.event.pull_request.title }}"' >> mirror-workflow.yml
